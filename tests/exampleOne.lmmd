program MyRlike;

vars 
int: i, j, p, arreglo[10];
float: valor;
char: cJuan, cHola, cSample;

function int factorialization (int j)
vars 
int: i; 
{ 
    i = j + p; 
}

function float factor (int t)
vars 
int: z; 
{ 
    z = t + p; 
}

main()
{    
    read(p);
    
    for i = 1 to 10 do
    {
        arreglo = p + i;
    }
}